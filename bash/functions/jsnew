# JSNew

## Generates skeleton for a new javascript project (arg1 = project name)

jsnew() {
  mkdir $1
  cd $1
  git clone git@github.com:jasmine/jasmine.git jasmine-resources
  mv jasmine-resources/dist/jasmine-standalone-2.1.3.zip ./
  rm -rf jasmine-resources
  unzip jasmine-standalone-2.1.3.zip
  sed -e '/Player/d' -i.bak SpecRunner.html
  sed -e '/Song/d' -i.bak SpecRunner.html
  sed -e '/Helper/d' -i.bak SpecRunner.html
  rm src/* spec/* SpecRunner.html.bak jasmine-standalone-2.1.3.zip
  cd ..
}
