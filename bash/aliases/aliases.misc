# Misc aliases

##
## Destroys sandbox, recreates it, and navigates to it
##
alias toast='rm -rf ~/sandbox; mkdir ~/sandbox; cd ~/sandbox'

##
## Improve directory list output
##
## A = list all entries incl. hidden but NOT incl. '.' and '..'
## F = / after dir, * after exe, @ after symlink
## G = colorize
## g = suppress owner
## o = suppress group
## h = humanize filesizes
## l = one entry per line
##
alias ll='ls -AFGgohl'
