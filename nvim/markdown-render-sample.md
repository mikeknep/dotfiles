# Heading 1

## Heading 2

Regular paragraph text with **bold**, *italic*, and ~~strikethrough~~.

Here is a [link to github](https://github.com).

> Blockquote line one
>
> Blockquote line two with **bold** and *italic*.

Inline code looks like `let x = 1`.

```lua
local message = "lua fenced code"
print(message)
```

```python
def greet(name: str) -> None:
    print(f"hello, {name}")
```

```json
{
  "feature": "markdown render sample",
  "enabled": true
}
```
